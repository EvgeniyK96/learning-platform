/* Ссылки на корневые DOM-узлы приложения.
   Модули загружаются с defer (type="module"), поэтому элементы уже в DOM. */
export const app = document.getElementById("app");
export const navAuth = document.getElementById("nav-auth");

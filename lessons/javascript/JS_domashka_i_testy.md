# Домашние задания и мини-тесты к курсу «JAVASCRIPT С НУЛЯ»
### Онлайн-школа Code Way

Документ содержит домашние задания к каждому из 50 уроков и мини-тесты по каждому из 9 модулей (Модуль 10 — итоговый проект, вместо теста используется защита перед куратором/группой).

---

# ЧАСТЬ 1. ДОМАШНИЕ ЗАДАНИЯ К КАЖДОМУ УРОКУ

## Модуль 1. Введение в JavaScript и настройка окружения

| Урок | Домашнее задание |
|------|-------------------|
| 1.1 Что такое JavaScript | Своими словами (5–7 предложений) опиши 3 сферы применения JavaScript и приведи по одному примеру реального продукта для каждой. |
| 1.2 Настройка окружения | Установи VS Code и расширение Live Server, создай папку проекта с файлами `index.html` и `script.js`, открой страницу через Live Server, пришли скриншот. |
| 1.3 Подключение JS к HTML | Подключи `script.js` к `index.html` внешним файлом и выведи в консоль три значения: своё имя, возраст и город, используя `console.log()`. |
| 1.4 Первая программа | Напиши программу: через `prompt()` спроси у пользователя имя, а затем выведи приветствие с этим именем через `console.log()` и через `alert()`. |

## Модуль 2. Основы синтаксиса

| Урок | Домашнее задание |
|------|-------------------|
| 2.1 var, let, const | Опиши себя тремя переменными: одна через `let` (то, что может измениться, например возраст), две через `const` (то, что не меняется, например имя и дата рождения). Объясни в комментарии, почему выбран именно такой тип переменной для каждой. |
| 2.2 Типы данных и typeof | Создай переменные всех примитивных типов (`number`, `string`, `boolean`, `undefined`, `null`) и выведи для каждой результат `typeof`. |
| 2.3 Операторы | Напиши мини-калькулятор: пользователь вводит два числа через `prompt()`, программа выводит сумму, разность, произведение, частное и результат сравнения (больше/меньше/равно). |
| 2.4 Приведение типов | Напиши 5 примеров выражений с неявным приведением типов (например, `"5" + 3`, `"5" - 3`, `true + 1`) и в комментариях объясни результат каждого. |
| 2.5 Шаблонные строки | Напиши программу «чек из магазина»: с помощью шаблонных строк выведи список из 3 товаров с ценами и итоговую сумму. |

## Модуль 3. Управляющие конструкции

| Урок | Домашнее задание |
|------|-------------------|
| 3.1 if/switch | Напиши функцию, определяющую по оценке (0–100) буквенную отметку через `if/else if/else`, и функцию, определяющую тип дня недели (будний/выходной) через `switch`. |
| 3.2 Цикл for | Выведи таблицу умножения для числа, введённого пользователем (от 1 до 10), используя цикл `for`. |
| 3.3 while/do...while | Реализуй игру «Угадай число» с использованием `do...while`: компьютер загадывает число от 1 до 100, пользователь угадывает его через `prompt()`. |
| 3.4 break/continue | Напечатай в консоли треугольник из звёздочек заданной высоты с помощью вложенных циклов, используй `break` для остановки при превышении заданной ширины. |
| 3.5 Практика | Реализуй классическую задачу FizzBuzz для чисел от 1 до 100. |

## Модуль 4. Функции

| Урок | Домашнее задание |
|------|-------------------|
| 4.1 Function declaration/expression | Напиши одну и ту же функцию `isPrime(n)` двумя способами: как function declaration и как function expression. Проверь и опиши в комментарии, как ведёт себя каждый вариант при вызове до объявления. |
| 4.2 Параметры, defaults, rest | Напиши функцию `total(...numbers)`, суммирующую любое количество чисел, и функцию `greet(name, greeting = "Привет")` со значением по умолчанию. |
| 4.3 Стрелочные функции | Перепиши 3 обычные функции в стрелочные и используй одну из них внутри `.map()` для массива чисел. |
| 4.4 Замыкания | Реализуй функцию `createCounter()`, возвращающую функцию-счётчик, которая при каждом вызове увеличивает и возвращает внутреннее значение (используя замыкание). |
| 4.5 Практика | Реализуй мини-калькулятор с отдельными функциями для сложения, вычитания, умножения и деления, выбор операции через `switch`. |

## Модуль 5. Массивы и объекты

| Урок | Домашнее задание |
|------|-------------------|
| 5.1 Массивы | Дан массив из 10 чисел — с помощью методов массива найди максимум, минимум и сумму без циклов `for`. |
| 5.2 map, filter, reduce | Дан массив чисел от 1 до 20 — с помощью `filter` и `map` получи массив квадратов чётных чисел, с помощью `reduce` посчитай их сумму. |
| 5.3 Объекты | Создай объект `car` с полями `brand`, `model`, `year` и методом `info()`, выводящим информацию о машине. |
| 5.4 Деструктуризация | Дан объект с данными о пользователе — извлеки 3 поля через деструктуризацию, включая одно с переименованием и одно со значением по умолчанию. |
| 5.5 Spread/rest | Объедини два массива и два объекта с помощью spread-оператора, создай копию массива и измени её, не затронув оригинал. |
| 5.6 Практика | Дан массив объектов `students` — получи имена студентов старше 20 лет и посчитай среднюю оценку по массиву. |

## Модуль 6. DOM: работа со страницей

| Урок | Домашнее задание |
|------|-------------------|
| 6.1 Что такое DOM | Открой любую веб-страницу, через консоль DevTools выведи `document.title` и `document.body`, опиши в комментарии, что ты увидел. |
| 6.2 querySelector | На простой HTML-странице с 3 разными элементами (id, class, тег) найди каждый из них через `querySelector` и выведи в консоль. |
| 6.3 Изменение контента и стилей | Напиши кнопку, при клике на которую меняется текст и цвет фона заголовка на странице. |
| 6.4 Создание/удаление элементов | Напиши код, создающий 5 элементов `<li>` с числами от 1 до 5 и добавляющий их в список на странице. |
| 6.5 Классы и атрибуты | Создай карточки товаров с `data-price`, посчитай их суммарную стоимость через `dataset`. |
| 6.6 Практика | Реализуй список задач (как в примере урока), добавь возможность отмечать задачу выполненной по клику. |

## Модуль 7. События и интерактивность

| Урок | Домашнее задание |
|------|-------------------|
| 7.1 addEventListener | Сделай кнопку-счётчик: при каждом клике число на странице увеличивается на 1. |
| 7.2 Мышь и клавиатура | Напиши обработчик, который выводит в консоль код нажатой клавиши, и отдельно реагирует на нажатие Enter. |
| 7.3 Всплытие событий | Реализуй список задач, где удаление элемента происходит через один обработчик на родителе (делегирование событий), а не отдельный обработчик на каждый li. |
| 7.4 Формы | Создай форму регистрации (имя, email, пароль) с валидацией при отправке: все поля обязательны, пароль не короче 8 символов. |
| 7.5 Практика | Дополни форму из Урока 7.4 валидацией в реальном времени (по событию blur) с выводом ошибок под каждым полем. |

## Модуль 8. Асинхронность

| Урок | Домашнее задание |
|------|-------------------|
| 8.1 Callback-функции | Напиши функцию `delayedGreeting(name, delay)`, которая через `setTimeout` выводит приветствие с заданной задержкой. |
| 8.2 Callback hell | Напиши 3 функции с callback, имитирующие последовательные шаги (например, загрузка → обработка → сохранение), вызови их вложенно и опиши в комментарии, почему такой код сложно читать. |
| 8.3 Promises | Перепиши функции из задания 8.2 так, чтобы каждая возвращала Promise, и вызови их цепочкой через `.then()`. |
| 8.4 async/await | Перепиши цепочку `.then()` из задания 8.3 с использованием `async/await`. |
| 8.5 Обработка ошибок | Добавь в функцию из задания 8.4 обработку ошибок через `try/catch`: если один из шагов «падает» (например, случайное число меньше 0.5), выводи понятное сообщение об ошибке. |

## Модуль 9. Работа с API (fetch) и JSON

| Урок | Домашнее задание |
|------|-------------------|
| 9.1 JSON | Сохрани массив из 3 объектов (например, задачи) в `localStorage` через `JSON.stringify`, затем загрузи и выведи его через `JSON.parse`. |
| 9.2 fetch GET | Получи данные с публичного тестового API (например, jsonplaceholder.typicode.com/users) и выведи имена всех пользователей на страницу. |
| 9.3 fetch POST | Отправь POST-запрос на jsonplaceholder.typicode.com/posts с телом `{title, body}` и выведи в консоль ответ сервера. |
| 9.4 Практика | Дополни приложение «Поиск стран» (или аналогичное) обработкой состояния загрузки и ошибок (город/страна не найдены, нет соединения). |

## Модуль 10. Итоговый проект
*Задания этого модуля — контрольные точки при разработке итогового проекта, а не домашние работы в привычном смысле.*

| Урок | Задание-контрольная точка |
|------|-----------------------------|
| 10.1 Планирование проекта | Оформи техническое задание: идея, функции, используемые темы курса, способ хранения данных. |
| 10.2 Структура приложения | Раздели проект на файлы (HTML/CSS/JS) и опиши заглушки будущих функций с комментариями. |
| 10.3 Реализация функционала | Реализуй базовую рабочую версию проекта согласно ТЗ. |
| 10.4 Тестирование проекта | Проверь проект на разных сценариях и граничных случаях, найди и исправь минимум 3 бага. |
| 10.5 Презентация проекта | Подготовь короткую презентацию (демонстрация + технические детали + возможные улучшения). |

---

# ЧАСТЬ 2. МИНИ-ТЕСТЫ ПО МОДУЛЯМ

*Формат: 5 вопросов с выбором одного правильного ответа из четырёх. Ответы — в конце каждого теста.*

## Тест по Модулю 1

1. Чем JavaScript изначально отличался от других языков программирования?
 a) выполнялся только на сервере
 b) был создан для интерактивности веб-страниц в браузере
 c) требовал обязательной компиляции
 d) не поддерживал переменные

2. Какая технология позволяет выполнять JavaScript вне браузера?
 a) HTML5
 b) Node.js
 c) CSS3
 d) JSON

3. Какой тег используется для подключения внешнего JS-файла к HTML-странице?
 a) `<javascript>`
 b) `<script src="...">`
 c) `<js>`
 d) `<link rel="js">`

4. Где рекомендуется размещать тег `<script>` без атрибута defer?
 a) в начале `<head>`
 b) перед закрывающим тегом `</body>`
 c) не имеет значения
 d) только в отдельном HTML-файле

5. Какая функция чаще всего используется для вывода данных в консоль разработчика?
 a) `print()`
 b) `console.log()`
 c) `output()`
 d) `echo()`

**Ответы:** 1-b, 2-b, 3-b, 4-b, 5-b

---

## Тест по Модулю 2

1. Какой способ объявления переменной считается устаревшим?
 a) let
 b) const
 c) var
 d) все актуальны одинаково

2. Что вернёт `typeof null`?
 a) "null"
 b) "undefined"
 c) "object"
 d) "boolean"

3. Что выведет `console.log("5" + 3)`?
 a) 8
 b) "53"
 c) ошибка
 d) 53

4. Какой оператор сравнения не выполняет приведение типов?
 a) `==`
 b) `===`
 c) `=`
 d) `!=`

5. Как записываются переменные и выражения внутри шаблонной строки?
 a) `{переменная}`
 b) `%переменная%`
 c) `${переменная}`
 d) `#{переменная}`

**Ответы:** 1-c, 2-c, 3-b, 4-b, 5-c

---

## Тест по Модулю 3

1. Что обязательно нужно писать после каждого `case` в конструкции switch, чтобы избежать "провала" в следующий блок?
 a) `stop`
 b) `break`
 c) `end`
 d) `return`

2. Сколько раз минимум выполнится тело цикла `do...while`, даже если условие изначально ложно?
 a) 0
 b) 1
 c) 2
 d) бесконечно

3. Какой цикл предпочтительнее использовать для перебора элементов массива?
 a) for...in
 b) for...of
 c) while(true)
 d) do...of

4. Что делает оператор `continue` внутри цикла?
 a) полностью останавливает цикл
 b) пропускает текущую итерацию и переходит к следующей
 c) перезапускает цикл с начала
 d) ничего не делает

5. Какая задача классически используется для проверки навыков работы с циклами и условиями на собеседованиях?
 a) Hello World
 b) FizzBuzz
 c) TODO List
 d) Calculator

**Ответы:** 1-b, 2-b, 3-b, 4-b, 5-b

---

## Тест по Модулю 4

1. Какое ключевое отличие function declaration от function expression?
 a) function declaration нельзя вызывать вообще
 b) function declaration поднимается (hoisting) и доступна до места объявления в коде
 c) function expression работает быстрее
 d) отличий нет

2. Какой синтаксис используется для сбора произвольного числа аргументов функции в массив?
 a) `**kwargs`
 b) `...args`
 c) `[args]`
 d) `{args}`

3. Что возвращает функция, если в ней нет оператора return?
 a) 0
 b) пустую строку
 c) undefined
 d) ошибку

4. Что такое замыкание (closure)?
 a) способ закрыть браузер
 b) функция, которая запоминает переменные из окружения, в котором была создана
 c) специальный тип цикла
 d) ошибка в коде

5. Как записывается стрелочная функция с одним параметром и одним возвращаемым выражением в сокращённом виде?
 a) `function(x) { return x*2 }`
 b) `x => x*2`
 c) `x -> x*2`
 d) `lambda x: x*2`

**Ответы:** 1-b, 2-b, 3-c, 4-b, 5-b

---

## Тест по Модулю 5

1. С какого индекса начинается нумерация элементов массива?
 a) с 1
 b) с 0
 c) с -1
 d) индексации нет

2. Какой метод массива создаёт новый массив, применяя функцию к каждому элементу?
 a) filter
 b) reduce
 c) map
 d) sort

3. Какой метод массива "сворачивает" его в одно итоговое значение?
 a) map
 b) filter
 c) reduce
 d) forEach

4. Как обратиться к значению свойства "age" объекта student двумя разными способами?
 a) student.age и student["age"]
 b) только student.age
 c) student->age и student.age
 d) student(age)

5. Что делает spread-оператор (`...`) с массивом?
 a) удаляет все элементы
 b) "распаковывает" элементы массива
 c) сортирует массив
 d) превращает массив в строку

**Ответы:** 1-b, 2-c, 3-c, 4-a, 5-b

---

## Тест по Модулю 6

1. Что такое DOM?
 a) язык программирования
 b) представление HTML-страницы в виде дерева объектов
 c) формат передачи данных
 d) библиотека JavaScript

2. Какой метод возвращает первый элемент, соответствующий CSS-селектору?
 a) getElementById
 b) querySelector
 c) querySelectorAll
 d) getElementsByClassName

3. Какое свойство безопаснее использовать для вставки простого текста, чтобы избежать риска XSS?
 a) innerHTML
 b) textContent
 c) outerHTML
 d) value

4. Какой метод создаёт новый DOM-элемент в памяти?
 a) document.newElement()
 b) document.createElement()
 c) document.addElement()
 d) document.makeElement()

5. Через какое свойство удобно обращаться к data-атрибутам элемента?
 a) element.data
 b) element.attributes
 c) element.dataset
 d) element.custom

**Ответы:** 1-b, 2-b, 3-b, 4-b, 5-c

---

## Тест по Модулю 7

1. Какой метод используется для назначения обработчика события на элемент?
 a) element.onEvent()
 b) element.addEventListener()
 c) element.handleEvent()
 d) element.setEvent()

2. Что содержит объект event.target внутри обработчика?
 a) название события
 b) элемент, на котором произошло событие
 c) время события
 d) координаты курсора

3. Какой метод отменяет поведение браузера по умолчанию (например, переход по ссылке)?
 a) event.stop()
 b) event.cancel()
 c) event.preventDefault()
 d) event.block()

4. Как называется механизм, при котором событие "поднимается" от целевого элемента к его родителям?
 a) погружение
 b) всплытие (bubbling)
 c) делегирование
 d) захват

5. Какое событие срабатывает при отправке HTML-формы?
 a) send
 b) submit
 c) post
 d) form

**Ответы:** 1-b, 2-b, 3-c, 4-b, 5-b

---

## Тест по Модулю 8

1. Как называется функция, переданная в другую функцию для вызова после завершения операции?
 a) main-функция
 b) callback-функция
 c) root-функция
 d) init-функция

2. В каком состоянии Promise находится, пока асинхронная операция ещё выполняется?
 a) fulfilled
 b) rejected
 c) pending
 d) resolved

3. Какое ключевое слово приостанавливает выполнение async-функции до завершения Promise?
 a) wait
 b) await
 c) pause
 d) stop

4. Какая конструкция используется для обработки ошибок в async/await коде?
 a) if/else
 b) try/catch
 c) switch/case
 d) do/while

5. Как называется проблема глубокой вложенности callback-функций?
 a) callback loop
 b) callback hell
 c) promise chain
 d) async trap

**Ответы:** 1-b, 2-c, 3-b, 4-b, 5-b

---

## Тест по Модулю 9

1. Что означает аббревиатура JSON?
 a) Java Standard Object Notation
 b) JavaScript Object Notation
 c) Java Source Open Network
 d) JavaScript Order Node

2. Какой метод преобразует объект JavaScript в JSON-строку?
 a) JSON.parse()
 b) JSON.stringify()
 c) JSON.convert()
 d) JSON.toString()

3. Какая встроенная функция используется для выполнения HTTP-запросов в браузере?
 a) request()
 b) fetch()
 c) load()
 d) get()

4. Какое свойство объекта response нужно проверять вручную, чтобы понять, успешен ли ответ сервера?
 a) response.success
 b) response.ok
 c) response.status === "success"
 d) response.done

5. Какой метод HTTP используется для отправки новых данных на сервер?
 a) GET
 b) POST
 c) HEAD
 d) OPTIONS

**Ответы:** 1-b, 2-b, 3-b, 4-b, 5-b

---

*Модуль 10 (итоговый проект) без теста — вместо него защита проекта перед куратором/группой по чек-листу из Урока 10.4–10.5.*

# Домашние задания и мини-тесты к курсу «JAVA С НУЛЯ»
### Онлайн-школа Code Way

Документ содержит домашние задания к каждому из 51 урока и мини-тесты по каждому из 9 модулей (Модуль 10 — итоговый проект, вместо теста используется защита перед куратором/группой).

---

# ЧАСТЬ 1. ДОМАШНИЕ ЗАДАНИЯ К КАЖДОМУ УРОКУ

## Модуль 1. Введение в Java и настройка окружения

| Урок | Домашнее задание |
|------|-------------------|
| 1.1 Что такое Java | Своими словами (5–7 предложений) опиши принцип «Write Once, Run Anywhere» и роль JVM в его реализации. |
| 1.2 Установка JDK | Установи JDK и IntelliJ IDEA, создай новый проект, выполни `java --version` и `javac --version`, пришли скриншот. |
| 1.3 Hello, World! | Напиши и запусти программу, выводящую на трёх строках имя, возраст и город через System.out.println. |
| 1.4 Структура проекта | Создай проект с пакетом `com.codeway.homework`, размести в нём класс с методом main, выводящим приветствие. |

## Модуль 2. Основы синтаксиса

| Урок | Домашнее задание |
|------|-------------------|
| 2.1 Переменные и типы | Объяви переменные всех примитивных типов, выведи их значения, продемонстрируй переполнение int через Integer.MAX_VALUE + 1. |
| 2.2 Scanner | Напиши программу, запрашивающую имя (nextLine) и возраст (nextInt) с учётом особенности смешивания этих методов. |
| 2.3 Операторы | Продемонстрируй разницу между == и .equals() для сравнения строк, созданных через new String(). |
| 2.4 Приведение типов | Покажи проблему целочисленного деления (int/int) и её решение через явное приведение к double. |
| 2.5 Обёрточные классы | Используй Integer.parseInt() для преобразования пользовательского ввода и объяви константу через static final. |

## Модуль 3. Управляющие конструкции

| Урок | Домашнее задание |
|------|-------------------|
| 3.1 if/switch | Напиши метод определения оценки через if/else if/else и метод определения типа дня через современный switch-выражение. |
| 3.2 Цикл for | Выведи таблицу умножения для числа, введённого пользователем, используя for-each для дополнительного массива. |
| 3.3 while/do-while | Реализуй игру «Угадай число» через do...while. |
| 3.4 break/continue | Реализуй вложенные циклы с использованием именованных меток для выхода из обоих циклов при определённом условии. |
| 3.5 Практика | Реализуй FizzBuzz для чисел от 1 до 100. |

## Модуль 4. Методы (функции)

| Урок | Домашнее задание |
|------|-------------------|
| 4.1 Объявление методов | Напиши статический метод isPrime(int n) и вызови его до места объявления в файле, чтобы убедиться, что порядок не важен. |
| 4.2 Параметры и возврат | Напиши метод, возвращающий массив из двух значений (мин и макс), продемонстрируй изменение содержимого массива внутри метода. |
| 4.3 Перегрузка методов | Напиши 2-3 перегруженные версии метода area() для разных фигур. |
| 4.4 Рекурсия | Реализуй рекурсивные методы для факториала (тип long) и чисел Фибоначчи. |
| 4.5 Практика | Реализуй мини-калькулятор с отдельными методами и выбором операции через switch-выражение. |

## Модуль 5. Массивы и строки

| Урок | Домашнее задание |
|------|-------------------|
| 5.1 Массивы | Дан массив из 10 чисел — найди сумму, максимум и минимум, продемонстрируй ArrayIndexOutOfBoundsException при выходе за границы. |
| 5.2 String | Продемонстрируй неизменяемость String на примере метода .concat(), сравни строки через == и .equals(). |
| 5.3 StringBuilder | Построй строку из 20 чисел через StringBuilder.append() в цикле, разверни строку через .reverse(). |
| 5.4 Arrays | Отсортируй массив через Arrays.sort(), найди элемент через Arrays.binarySearch(), выведи через Arrays.toString(). |
| 5.5 Практика | Реализуй проверку строки на анаграмму другой строки. |

## Модуль 6. Объектно-ориентированное программирование

| Урок | Домашнее задание |
|------|-------------------|
| 6.1 Классы и объекты | Создай класс Car с конструктором, использующим this для полей, и создай 2-3 объекта. |
| 6.2 Методы и модификаторы | Добавь в класс Car приватное поле mileage, публичный метод drive() и статический счётчик созданных объектов. |
| 6.3 Инкапсуляция | Сделай все поля класса Person приватными, добавь геттеры/сеттеры с валидацией возраста в setAge(). |
| 6.4 Наследование | Создай класс ElectricCar, наследующий от Car через extends, с вызовом super() в конструкторе. |
| 6.5 Полиморфизм | Создай абстрактный класс Shape с абстрактным методом area() и 2 наследника (Circle, Square). |
| 6.6 Интерфейсы | Создай 2 интерфейса (например, Flyable, Swimmable) и класс, реализующий оба через implements. |
| 6.7 toString/equals | Переопредели toString(), equals() и hashCode() для класса Point, продемонстрируй разницу между == и .equals(). |
| 6.8 Практика | Расширь пример «Автопарк»: добавь третий класс транспорта и метод подсчёта количества транспорта каждого типа. |

## Модуль 7. Коллекции (Java Collections Framework)

| Урок | Домашнее задание |
|------|-------------------|
| 7.1 ArrayList | Реализуй динамический список оценок через ArrayList<Double>, вычисли среднее значение. |
| 7.2 HashSet | Удали дубликаты из списка чисел через HashSet, продемонстрируй операции объединения и пересечения двух множеств. |
| 7.3 HashMap | Реализуй телефонный справочник через HashMap<String, String> с использованием getOrDefault(). |
| 7.4 Iterator | Удали из ArrayList все чётные числа безопасно через явный Iterator (не через for-each). |
| 7.5 Практика | Реализуй подсчёт частоты слов в тексте через HashMap<String, Integer> и вывод топ-3 самых частых слов. |

## Модуль 8. Исключения и работа с файлами

| Урок | Домашнее задание |
|------|-------------------|
| 8.1 try/catch/finally | Напиши пример с блоком finally, демонстрирующий его выполнение как при успехе, так и при исключении. |
| 8.2 Checked/unchecked | Напиши метод, объявленный с throws IOException, и вызови его с обязательной обработкой в try/catch. |
| 8.3 Собственные исключения | Создай собственное unchecked-исключение (например, InvalidAgeException extends RuntimeException) и используй его в классе Person. |
| 8.4 Файлы | Напиши программу, сохраняющую список из 5 фильмов в файл через FileWriter и считывающую его через BufferedReader. |
| 8.5 Практика | Реализуй программу учёта расходов с сохранением в файл и полной обработкой ошибок ввода и файловой системы. |

## Модуль 9. Работа с базами данных (JDBC)

| Урок | Домашнее задание |
|------|-------------------|
| 9.1 Введение в JDBC | Установи соединение с SQLite базой данных через DriverManager.getConnection(), выведи сообщение об успешном подключении. |
| 9.2 SQL-запросы | Создай таблицу, добавь 3 записи через executeUpdate(), выведи все записи через executeQuery() и ResultSet. |
| 9.3 PreparedStatement | Перепиши добавление записей из задания 9.2 с использованием PreparedStatement вместо Statement. |
| 9.4 Практика | Реализуй класс-репозиторий для сущности на твой выбор (например, товары, книги) с методами add, getAll, delete через JDBC. |

## Модуль 10. Итоговый проект
*Задания этого модуля — контрольные точки при разработке итогового проекта, а не домашние работы в привычном смысле.*

| Урок | Задание-контрольная точка |
|------|-----------------------------|
| 10.1 Планирование проекта | Оформи техническое задание: задача, классы, коллекции, хранение данных, обработка ошибок. |
| 10.2 Структура классов | Спроектируй модель данных, интерфейс репозитория и собственное исключение проекта. |
| 10.3 Реализация функционала | Реализуй репозиторий через JDBC и главный класс с меню согласно ТЗ. |
| 10.4 Тестирование проекта | Протестируй проект на разных сценариях и граничных случаях, найди и исправь минимум 3 бага. |
| 10.5 Презентация проекта | Подготовь короткую презентацию проекта (идея → демонстрация → технические детали → улучшения). |

---

# ЧАСТЬ 2. МИНИ-ТЕСТЫ ПО МОДУЛЯМ

*Формат: 5 вопросов с выбором одного правильного ответа из четырёх. Ответы — в конце каждого теста.*

## Тест по Модулю 1

1. Во что компилируется исходный код Java перед выполнением?
 a) напрямую в машинный код
 b) в байт-код
 c) в код на C
 d) ни во что, выполняется напрямую

2. Что выполняет байт-код Java на конкретной операционной системе?
 a) JDK
 b) JVM
 c) javac
 d) IDE

3. Что необходимо установить для разработки (а не только запуска) Java-программ?
 a) только JRE
 b) JDK
 c) только JVM
 d) ничего дополнительного не требуется

4. Какое имя обязательно должен иметь файл, содержащий публичный класс Main?
 a) любое
 b) Main.java
 c) main.txt
 d) Class.java

5. Какая команда компилирует Java-файл в байт-код?
 a) `java`
 b) `javac`
 c) `compile`
 d) `build`

**Ответы:** 1-b, 2-b, 3-b, 4-b, 5-b

---

## Тест по Модулю 2

1. Сколько примитивных типов данных в Java?
 a) 5
 b) 6
 c) 8
 d) 10

2. Какой суффикс обязателен для литерала типа long, превышающего диапазон int?
 a) `l` в любом регистре не нужен
 b) `L`
 c) `Lo`
 d) `long`

3. Что произойдёт при сравнении двух строк, созданных через new String(), с помощью ==?
 a) вернёт true, если содержимое одинаково
 b) вернёт false, так как это разные объекты в памяти
 c) вызовет ошибку компиляции
 d) всегда вернёт true

4. Какой метод правильно сравнивает содержимое двух строк?
 a) `==`
 b) `.equals()`
 c) `.compare()`
 d) `.same()`

5. Какое ключевое слово используется для объявления константы в Java?
 a) `const`
 b) `final`
 c) `static`
 d) `readonly`

**Ответы:** 1-c, 2-b, 3-b, 4-b, 5-b

---

## Тест по Модулю 3

1. С какими типами данных может работать switch в Java (в отличие от классического C++)?
 a) только с int
 b) с int, char и дополнительно со String
 c) только с boolean
 d) ни с чем, switch отсутствует в Java

2. Какой современный синтаксис switch-выражения не требует break?
 a) switch с case и двоеточием
 b) switch со стрелкой ->
 c) switch с if внутри
 d) такого синтаксиса не существует

3. Что позволяют делать именованные метки в Java, в отличие от классического C++?
 a) создавать переменные
 b) управлять break/continue во вложенных циклах напрямую
 c) объявлять константы
 d) ничего особенного

4. Сколько раз минимум выполнится тело цикла do...while?
 a) 0
 b) 1
 c) зависит от условия
 d) всегда бесконечно

5. Какое свойство массива хранит его длину в Java?
 a) `.size()`
 b) `.length`
 c) `.count()`
 d) `.len`

**Ответы:** 1-b, 2-b, 3-b, 4-b, 5-b

---

## Тест по Модулю 4

1. Могут ли в Java существовать функции вне классов?
 a) да, как и в Python
 b) нет, любой метод обязательно принадлежит классу
 c) только в отдельных файлах
 d) только static методы могут быть вне класса

2. Как передаются примитивные типы в методы Java?
 a) по ссылке
 b) только по значению
 c) в зависимости от типа
 d) через указатели

3. Что происходит при передаче массива в метод в Java?
 a) передаётся полная копия массива
 b) передаётся копия ссылки, изменения элементов влияют на оригинал
 c) массивы нельзя передавать в методы
 d) происходит ошибка компиляции

4. Какой синтаксис используется для метода с переменным числом аргументов?
 a) `*args`
 b) `тип... имя`
 c) `[]args`
 d) `...args`

5. Какая ошибка возникает при слишком глубокой рекурсии в Java?
 a) `RecursionError`
 b) `StackOverflowError`
 c) `DeepRecursionException`
 d) `MemoryError`

**Ответы:** 1-b, 2-b, 3-b, 4-b, 5-b

---

## Тест по Модулю 5

1. Чем являются массивы в Java с точки зрения типов?
 a) примитивами
 b) полноценными объектами
 c) указателями
 d) специальным синтаксисом без типа

2. Что произойдёт при обращении к индексу за пределами массива в Java?
 a) неопределённое поведение, как в C++
 b) выбросится ArrayIndexOutOfBoundsException
 c) программа автоматически расширит массив
 d) вернётся null

3. Какая ключевая особенность класса String в Java?
 a) он изменяем (mutable)
 b) он неизменяем (immutable)
 c) он является примитивом
 d) он не поддерживает методы

4. Какой класс рекомендуется для эффективного построения строк в цикле?
 a) String
 b) StringBuilder
 c) StringArray
 d) TextBuilder

5. Какой метод класса Arrays выводит читаемое содержимое массива?
 a) `Arrays.print()`
 b) `Arrays.toString()`
 c) `Arrays.show()`
 d) `Arrays.display()`

**Ответы:** 1-b, 2-b, 3-b, 4-b, 5-b

---

## Тест по Модулю 6

1. Какое ключевое слово используется для наследования класса в Java?
 a) `inherits`
 b) `extends`
 c) `:`
 d) `implements`

2. Сколько классов может одновременно наследовать один класс в Java?
 a) неограниченно
 b) только один (единичное наследование)
 c) два
 d) наследование запрещено

3. Что компенсирует отсутствие множественного наследования классов в Java?
 a) абстрактные классы
 b) интерфейсы
 c) статические методы
 d) ничего не компенсирует

4. Нужно ли в Java явно указывать virtual для полиморфного поведения метода?
 a) да, как в C++
 b) нет, все нестатические методы полиморфны по умолчанию
 c) да, но только для абстрактных классов
 d) полиморфизм в Java не поддерживается

5. Какое правило обязательно при переопределении equals()?
 a) должен быть переопределён и toString()
 b) должен быть переопределён и hashCode()
 c) класс должен стать final
 d) никаких дополнительных правил нет

**Ответы:** 1-b, 2-b, 3-b, 4-b, 5-b

---

## Тест по Модулю 7

1. Что хранит ArrayList, в отличие от обычного массива?
 a) только примитивы
 b) объекты, с возможностью динамически менять размер
 c) только строки
 d) фиксированное количество элементов

2. Какая коллекция хранит только уникальные элементы?
 a) List
 b) Set
 c) Map
 d) Array

3. Что возвращает метод .getOrDefault() у HashMap при отсутствии ключа?
 a) выбрасывает исключение
 b) значение по умолчанию, указанное вторым аргументом
 c) всегда null
 d) удаляет ключ

4. Какая ошибка возникает при изменении коллекции напрямую во время перебора через for-each?
 a) `ArrayIndexOutOfBoundsException`
 b) `ConcurrentModificationException`
 c) `NullPointerException`
 d) ничего не происходит

5. Какой метод Iterator безопасно удаляет текущий элемент во время перебора?
 a) `.delete()`
 b) `.remove()`
 c) `.removeCurrent()`
 d) `.pop()`

**Ответы:** 1-b, 2-b, 3-b, 4-b, 5-b

---

## Тест по Модулю 8

1. Какой блок в Java выполняется независимо от того, было исключение или нет?
 a) catch
 b) finally
 c) throws
 d) default

2. Какие исключения обязательно требуют обработки или объявления через throws?
 a) unchecked
 b) checked
 c) все исключения без исключения
 d) никакие

3. От какого класса наследуются unchecked-исключения?
 a) Exception напрямую
 b) RuntimeException
 c) Error
 d) Throwable напрямую, минуя Exception

4. Какая конструкция автоматически закрывает ресурсы (файлы и т.д.) по завершении блока?
 a) try/catch
 b) try-with-resources
 c) finally-block
 d) auto-close

5. Какое исключение обычно связано с операциями ввода-вывода файлов?
 a) `SQLException`
 b) `IOException`
 c) `ArithmeticException`
 d) `ArrayIndexOutOfBoundsException`

**Ответы:** 1-b, 2-b, 3-b, 4-b, 5-b

---

## Тест по Модулю 9

1. Что такое JDBC?
 a) язык запросов к базе данных
 b) стандартный API Java для взаимодействия с базами данных
 c) отдельная база данных
 d) фреймворк для веб-разработки

2. Какой метод используется для выполнения запроса SELECT?
 a) `executeUpdate()`
 b) `executeQuery()`
 c) `execute()` только
 d) `select()`

3. Что возвращает executeQuery()?
 a) количество затронутых строк
 b) объект ResultSet
 c) булево значение
 d) ничего не возвращает

4. Почему PreparedStatement безопаснее обычного Statement при работе с пользовательским вводом?
 a) он быстрее выполняется
 b) он защищает от SQL-инъекций через параметризацию запроса
 c) он не требует SQLException
 d) отличий в безопасности нет

5. С какого индекса начинается нумерация параметров ? в PreparedStatement?
 a) с 0
 b) с 1
 c) с -1
 d) индексация не нужна

**Ответы:** 1-b, 2-b, 3-b, 4-b, 5-b

---

*Модуль 10 (итоговый проект) без теста — вместо него защита проекта перед куратором/группой по чек-листу из Урока 10.4–10.5.*
